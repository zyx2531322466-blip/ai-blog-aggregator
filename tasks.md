# 任务清单（Tasks）

本文档从 `plan.md` 中提取具体可执行任务，按模块与实现顺序组织。

## 0. 项目初始化
- [ ] 初始化代码仓库，配置目录结构（backend / frontend / infra）
- [ ] 配置 Docker Compose（PostgreSQL、Redis、API 服务、前端服务）
- [ ] 配置 GitHub Actions CI（lint、测试、构建）
- [ ] 配置代码规范工具（后端：Black/Flake8；前端：ESLint/Prettier）

## 1. 数据存储层
- [ ] 设计数据库表结构：articles（文章）、categories（分类）、sources（来源）、duplicate_relations（去重合并关系）
- [ ] 编写数据库迁移脚本（如使用 Alembic）
- [ ] 实现基础的数据访问层（Repository/ORM 模型，如 SQLAlchemy）

## 2. 爬虫模块（Crawler Service）
- [ ] 实现站点配置管理（可配置的抓取源列表）
- [ ] 实现基础抓取器：请求目标 URL、遵守 robots.txt、限流控制
- [ ] 实现内容解析器：从 HTML 中提取标题、正文、发布时间、作者等字段
- [ ] 实现抓取任务的错误处理与重试机制
- [ ] 将抓取结果写入"待处理"表/队列
- [ ] 编写爬虫模块的单元测试（使用录制的 HTML fixtures）

## 3. 分类模块（Classifier）
- [ ] 定义初始分类体系（类别列表）
- [ ] 实现基于关键词/规则的初版分类器
- [ ] 实现分类结果写入文章记录的逻辑
- [ ] 实现"未分类/其他"兜底逻辑
- [ ] 编写分类模块的单元测试
- [ ] （后续迭代）调研并接入基于语义向量的分类方案

## 4. 去重模块（Deduplicator）
- [ ] 实现精确去重：内容哈希计算与比对
- [ ] 实现近似去重：SimHash/MinHash 指纹计算
- [ ] 实现重复判定阈值的配置与调优机制
- [ ] 实现"合并关系"记录的写入（保留所有来源信息）
- [ ] 实现"同一事件不同视角"与"完全重复"的区分逻辑
- [ ] 编写去重模块的单元测试（含边界案例）

## 5. 任务调度
- [ ] 集成 Celery + Redis 作为任务队列
- [ ] 实现周期性抓取任务的调度（定时触发各来源的爬取）
- [ ] 实现抓取 → 分类 → 去重 的任务编排（pipeline）
- [ ] 添加任务执行的日志记录与失败告警

## 6. 后端 API（FastAPI）
- [ ] 实现 `GET /api/v1/articles`（列表、筛选、分页）
- [ ] 实现 `GET /api/v1/articles/{id}`（详情，含合并来源列表）
- [ ] 实现 `GET /api/v1/categories`（分类列表与计数）
- [ ] 实现管理员鉴权机制（Token/JWT）
- [ ] 实现 `POST /api/v1/admin/sources`（新增/管理采集来源）
- [ ] 实现 `GET /api/v1/admin/insights`（分类分布、去重统计概览）
- [ ] 实现统一错误响应格式
- [ ] 编写 API 层的集成测试
- [ ] 生成并校验 OpenAPI 文档

## 7. 前端（React + TypeScript）
- [ ] 搭建项目脚手架，接入 Tailwind CSS 设计规范
- [ ] 实现文章列表页（分类筛选、时间/来源排序、分页）
- [ ] 实现文章详情页（展示合并来源信息）
- [ ] 实现分类导航组件
- [ ] 实现加载中/空结果/请求失败等状态的统一提示组件
- [ ] 实现管理后台页面：采集来源配置、分类与去重效果概览
- [ ] 编写前端组件的单元测试

## 8. 可观测性与运维
- [ ] 集成 Prometheus 指标采集（爬取耗时、队列积压、API 响应时间）
- [ ] 搭建 Grafana 监控看板
- [ ] 配置关键指标告警规则

## 9. 测试与质量保障
- [ ] 补齐核心模块（分类、去重）单元测试，达到覆盖率门槛
- [ ] 编写端到端集成测试（爬取 → 分类 → 去重 → API → 前端展示）
- [ ] 建立分类/去重结果的人工抽检流程

## 10. 上线准备
- [ ] 编写部署文档与环境配置说明
- [ ] 配置生产环境的 CI/CD 部署流水线
- [ ] 进行上线前的性能与压力测试

---

# v2 增补任务（客户新增需求）

> 来源：`spec.md` 第二、三章的 v2 条款（用户故事 10–16），技术方案见 `plan.md` 的 v2 章节。
> 决策前提：**邮件推送** · **匿名订阅（邮箱 + 双确认）** · **全局知识库级去重** · **外部 LLM API**。

## 11. 订阅与定期邮件推送
- [ ] 设计并迁移订阅相关表：`subscriptions`、`subscription_topics`、`digests`、`digest_items`、`notification_settings`
- [ ] 实现"可订阅方向"清单接口（类别 / 标签 / 来源 / 关键词 + 各自可选值）
- [ ] 实现订阅创建接口：邮箱 + 方向集合 + 节奏（daily/weekly）+ 条数上限 → 生成确认凭证，状态置为"待确认"
- [ ] 实现订阅确认接口（双确认）：校验凭证与有效期，状态置为"已确认"，记录确认时间
- [ ] 实现退订接口（一键退订 + 全部退订）：立即生效并记录退订原因（可选）
- [ ] 实现订阅数据自助查询/删除接口（凭邮箱 + 凭证，无需登录）
- [ ] 实现反滥用：同邮箱重复提交去重、单 IP/单邮箱频率限制、确认凭证过期清理、审计日志
- [ ] 实现方向匹配逻辑：文章命中订阅方向集合（类别/标签/来源/关键词任一）即视为相关，支持去重与优先级
- [ ] 实现摘要生成：按周期取新增相关文章 → 排除合并成员与知识重复 → 排除已推送 → 排序 → 截断上限
- [ ] 实现邮件模板（HTML + 纯文本双版本），含标题、要点/摘要、类别标签、来源、发布时间、原文链接、退订入口
- [ ] 实现邮件发送适配器（SMTP，接口化以便测试替换与后续切换到托管服务）
- [ ] 实现投递记录与失败处理：记录每封邮件的状态、错误信息、重试次数与重试时限
- [ ] 实现无更新周期的处理策略（不发送 / 发送提示邮件，可配置）
- [ ] 实现调度接入：按节奏触发摘要生成与投递，支持发送时间窗与"每订阅串行、可重入"
- [ ] 实现幂等保证：同一文章对同一订阅只推送一次（唯一约束 + 事务）
- [ ] 维护者接口：订阅列表（邮箱脱敏）、推送记录查询、手动触发一次推送、全局推送设置（节奏/时间窗/上限）
- [ ] 测试：订阅生命周期（创建/确认/过期/退订）、方向匹配、周期边界（含无更新）、幂等不重复推送、模板渲染、发送失败与重试、反滥用频率限制
- [ ] 测试中使用假发送器与本地邮件捕获（Mailpit/MailHog），**不得**在测试中真实发信

## 12. LLM Wiki 与知识级去重
- [ ] 设计并迁移知识相关表：`wiki_entries`、`wiki_entry_sources`、`knowledge_decisions`，并为条目增加向量列（pgvector）与索引
- [ ] 实现知识点提炼：调用外部 LLM 以 JSON 结构输出"知识点列表"（名称/摘要/关键主张），做 schema 校验与非法输出重试
- [ ] 实现文本向量化：对知识点摘要生成 embedding（同一 LLM 网关），并落库
- [ ] 实现相似度匹配：新知识点与 Wiki 活跃条目做余弦相似度检索（先向量召回 Top-K，再精确比较）
- [ ] 实现判定策略：高阈值→已覆盖；中间区间→LLM 二次判定；**无法确定一律判为"全新知识"**（保守优先）
- [ ] 实现判定记录：写入侵边条目、相似度、模型名与提示词版本、判定理由，支持按文章/条目回溯
- [ ] 实现文章处置：全新→正常展示；已覆盖→归档为"知识重复"（不进列表、不进推送）；部分新增→正常展示并把新增知识点写入 Wiki
- [ ] 实现与既有去重的优先级衔接：完全重复 → 近重复 → 知识级判定（前两级先合并，避免重复调用与误判）
- [ ] 实现"待判定"状态与重试：LLM 超时/限流/配额不足时不阻断流程，后续轮次继续处理
- [ ] 实现成本与用量控制：单篇与单轮调用额度、批量大小、并发上限、结果缓存，并提供调用量与失败率查询
- [ ] 实现人工维护接口：Wiki 条目新增/编辑/废止/合并（含合并后条目指向），人工备注
- [ ] 实现人工改判接口：把文章改判为"非知识重复"或"确认为知识重复"，人工结论优先于系统结论
- [ ] 实现"已废止条目不再作为覆盖依据"以及改判结果对后续判定的影响
- [ ] 实现知识去重统计：被筛掉的文章清单、按类别/来源分布、抽检抽样入口
- [ ] 测试：样本集判定（全新/已覆盖/部分新增）、阈值边界、LLM 故障与超时降级、人工改判优先级、幂等与重复处理、既有文本级去重回归

## 13. 前端与文档（v2）
- [ ] 实现订阅页：方向选择（类别/标签/来源/关键词）、节奏与条数上限、邮箱提交与"请查收确认邮件"提示
- [ ] 实现订阅确认结果页与退订结果页（含错误态：凭证无效/已退订）
- [ ] 实现"订阅数据查询/删除"入口（凭邮箱与凭证）
- [ ] 实现维护者后台：推送记录与投递明细、推送设置、Wiki 条目管理、知识判定复核与改判
- [ ] 在站点显著位置补充"内容会发送给外部 LLM 用于知识点提炼"的说明
- [ ] 更新文档：`README.md`（功能与接口一览）、`docs/sharing-guide.md`（邮件配置与域名要求）、`docs/convergence-report.md`（新增需求的收敛对照）
- [ ] 补充前端测试：订阅表单校验、确认/退订页面、维护者列表渲染与改判交互

## 14. 上线与合规（v2 增补）
- [ ] 配置发信域名与邮件认证：SPF、DKIM、DMARC，并完成发信测试（避免进垃圾箱）
- [ ] 配置退信/投诉处理流程与阈值告警（退信率、投诉率）
- [ ] 编写隐私说明：仅收集邮箱与订阅偏好、数据保留期限、删除方式、LLM 数据边界
- [ ] 配置监控指标：投递成功率、退信率、LLM 调用失败率与成本、知识去重误筛抽检结果
- [ ] 编写推送运营手册：批量推送限速、暂停开关、异常时的人工介入步骤
- [ ] 进行上线前演练：小规模真实邮箱灰度（先内部、再白名单、最后放开）

## 15. v2 实现步骤（建议顺序与依赖）

1. **基础表与配置**：迁移 11/12 两节涉及的 6 张表 + 向量列 + 全局设置（依赖：既有 Alembic 与设置表模式）
2. **订阅生命周期**（无邮件）：创建/确认/退订/查询接口 + 反滥用 → 可独立验收（验收点：用户故事 12）
3. **方向匹配与摘要生成**（不发信）：内部接口产出"某订阅本期应推送的文章清单" → 验收点：用户故事 10、11（内容正确性）
4. **邮件投递**：模板 + SMTP 适配器 + 投递记录 + 幂等 → 验收点：用户故事 11、13
5. **调度接入与时间窗**：按节奏自动运行、可重入、失败重试 → 验收点：用户故事 11、13
6. **知识点提炼与向量化**：LLM 调用 + schema 校验 + 待判定与重试 → 验收点：用户故事 14（前半）
7. **Wiki 与匹配判定**：条目表 + 相似度召回 + 保守判定 + 判定记录 → 验收点：用户故事 14、16
8. **处置与流水线衔接**：归档/展示/补条目 + 与文本级去重排序 → 验收点：用户故事 14（后半）
9. **人工维护与改判**：条目增删改合 + 文章改判 + 优先级规则 → 验收点：用户故事 15
10. **前端与文档**：订阅页、确认/退订页、维护者复核页、文档同步 → 验收点：用户故事 10–16 可用性
11. **合规与上线**：邮件认证、隐私说明、监控与灰度 → 验收点：可安全对外

> 依赖与并行建议：步骤 2–5（订阅推送）与步骤 6–9（知识去重）可并行开发；
> 但**步骤 5 的推送内容过滤依赖步骤 8 的知识判定结果**，联调时需按"先知识判定、后摘要生成"的顺序组装。

## 变更记录

| 版本 | 变更 | 影响章节 |
| --- | --- | --- |
| v1 | 项目初始化 → 上线准备（第 0–10 节） | 全文 |
| v2 | 新增第 11–14 节（订阅推送 / LLM Wiki 知识去重 / 前端与文档 / 上线合规）与第 15 节实现步骤 | 第 11–15 节 |

