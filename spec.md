# 功能需求说明（Spec）

本文档描述"自动爬取网络相关内容博客，并自动分类整理去重"网站的功能需求，聚焦于 **做什么（What）** 与 **为什么（Why）**，不涉及具体技术实现方式。

> **版本 v2（客户新增需求）**：在既有"采集 / 分类 / 去重 / 浏览 / 维护"能力之上，
> 新增两项需求：
> 1. **订阅感兴趣的方向并定期推送**——让用户不必反复回站查看，而是定期收到与自己相关的更新摘要；
> 2. **LLM Wiki 知识去重**——用一个持续维护的"已覆盖知识点"知识库，把"换汤不换药"的重复知识筛掉，
>    实现比文本级去重更进一步的**知识级去重**。
>
> v2 的关键决策（已确认）：推送渠道为**邮件**；订阅采用**匿名订阅（邮箱 + 双确认）**，仍**不引入账号体系**；
> 知识去重以**全局知识库级**为准；知识点提炼使用**外部 LLM API**。

## 一、用户故事

1. **作为一名内容浏览者**，我希望网站能自动帮我从网络上收集相关领域的博客文章，这样我就不需要自己去各处搜索和订阅。

2. **作为一名内容浏览者**，我希望看到的内容已经被自动分类整理好（按主题/领域归类），这样我可以只浏览自己感兴趣的类别，而不用翻阅所有文章。

3. **作为一名内容浏览者**，我希望重复或高度相似的文章（例如转载、镜像发布、同一事件的多篇报道）被自动识别并合并/去重，这样我不会反复看到相同的内容。

4. **作为一名内容浏览者**，我希望能够按分类、时间或来源筛选和浏览文章，以便快速找到我关心的内容。

5. **作为一名内容浏览者**，我希望知道每篇文章的原始来源和发布时间，以便判断内容的可信度与时效性，并在需要时访问原文。

6. **作为一名回访用户**，我希望网站能持续更新内容（而不是一次性抓取），这样我每次访问都能看到新增的相关文章。

7. **作为一名内容浏览者**，当去重系统合并了多篇相似文章时，我希望能看到这些文章都来自哪些源头，而不是只看到被随意选中的一篇。

8. **作为网站的维护者**，我希望能够管理和调整"相关内容"的抓取范围（例如添加/移除关注的站点或话题），以便网站内容始终符合预期方向。

9. **作为网站的维护者**，我希望能够了解分类是否准确、去重是否合理，以便在效果不理想时进行调整。

### v2 新增用户故事（订阅推送）

10. **作为一名内容浏览者**，我希望能订阅自己感兴趣的方向（类别、标签、来源或关键词），
    这样我收到的内容都与我相关，而不是全站所有文章。

11. **作为一名订阅者**，我希望按我选择的节奏（例如每天或每周）收到一封摘要邮件，
    里面列出这段时间内新增的相关文章及其要点与原文链接，这样我不必反复回站查看。

12. **作为一名订阅者**，我希望订阅前需要我本人确认、并且随时可以一键退订，
    这样我的邮箱不会被他人冒用，也不会被迫接收不想看的邮件。

13. **作为网站维护者**，我希望能控制推送的整体节奏与单封邮件的内容规模，并看到每次推送的送达情况，
    这样既能保证用户可读性，也能避免把站点变成垃圾邮件来源。

### v2 新增用户故事（LLM Wiki 知识去重）

14. **作为一名内容浏览者**，我希望系统能识别"知识层面重复"的文章——
    即已经讲过的知识点被换一种说法、换一个来源再写一遍——并把它从我的浏览与推送中筛掉，
    这样我的时间只花在真正有新信息的内容上。

15. **作为网站维护者**，我希望系统维护一份可读、可编辑的"知识 Wiki"（已经覆盖过的知识点清单），
    并且我能对系统判定错误的地方进行人工修正（补充、合并、废止条目，或改判某篇文章）。

16. **作为网站维护者**，我希望能看到"哪篇文章因为知识已覆盖而被筛掉、依据是什么"，
    以便评估这个能力是否过严或过松，并据此调整策略。

## 二、功能需求

### 内容采集
- 系统应能够从多个网络来源自动发现并采集与设定主题相关的博客文章。
- 系统应支持持续或周期性地采集新发布的内容，而不仅仅是一次性抓取。
- 系统应能识别并跳过明显不相关或质量过低的内容（如广告页、纯导航页）。
- 系统应记录每篇采集内容的原始来源地址与采集时间。

### 内容分类
- 系统应能根据内容主题自动将文章归入一个或多个类别。
- 分类结果应对用户可见，并支持用户按类别筛选浏览。
- 当一篇文章难以明确归类时，系统应有合理的兜底处理方式（如归入"未分类"或"其他"），而不是采集失败或丢弃。

### 去重与内容整理
- 系统应能识别内容高度相似或完全相同的文章（包括转载、镜像、微小改写等情况），并将其判定为重复。
- 对于被判定为重复的文章，系统应将其合并展示为一条记录，同时保留所有相关来源的可追溯信息。
- 系统应能区分"完全重复"与"同一事件的不同报道/视角"，避免误将有价值的不同视角内容错误合并。

### 内容浏览与展示
- 用户应能查看文章列表，并按分类、时间、来源等维度进行筛选与排序。
- 每篇文章的展示应包含标题、摘要、分类、来源、发布时间等关键信息。
- 用户应能通过点击跳转到原始文章来源。

### 内容质量与可维护性
- 网站维护者应能配置或调整内容采集的范围（关注的主题、站点）。
- 网站维护者应能查看分类与去重的整体效果（如分类分布、去重命中情况），以便发现并修正明显错误。

### 订阅与定期推送（v2 新增）
- 用户应能选择自己感兴趣的方向进行订阅；方向维度至少包括：类别、标签、来源、关键词。
- 订阅应以**邮箱**作为触达方式；用户提交邮箱后必须经过**本人确认**才生效（防止冒用他人邮箱）。
- 用户应能选择推送节奏（如"每天"或"每周"），并可设置单封邮件希望看到的条数上限。
- 用户应能在每封邮件中一键退订；退订应立即生效，且不需要登录或联系维护者。
- 系统应按节奏自动生成并发送摘要邮件，内容为"自上次推送以来、该用户订阅方向内、尚未推送过的"新增文章。
- 摘要邮件应包含：文章标题、要点或摘要、类别/标签、来源与发布时间，以及可点击的原文链接。
- 同一篇文章对同一订阅者**不应重复推送**；推送失败应被记录并可在下一次重试。
- 若某方向在周期内新增内容为 0，系统应按配置选择"不发送"或发送"本周期无更新"的提示邮件，避免无意义打扰。
- 系统应具备反滥用能力：对同一邮箱的重复订阅请求、异常高频请求做限制，并保留必要的审计记录。
- 邮件必须包含发送者身份说明与退订入口，符合常见反垃圾邮件要求；系统应能查询每个订阅的送达与失败情况。
- 维护者应能配置推送的全局默认节奏、发送时间窗（避免深夜打扰）与单封邮件的最大条数。
- 用户应能查询或删除自己的订阅数据（仅凭订阅时提供的邮箱与确认凭证，无需注册账号）。

### LLM Wiki 与知识级去重（v2 新增）
- 系统应维护一份**知识 Wiki**：由"知识点条目"构成，每个条目描述一个已经被覆盖过的知识点（名称、摘要、支撑文章、人工备注）。
- 系统应能借助 **LLM** 把新文章提炼为若干"知识点"，并与 Wiki 中已有条目进行比对。
- 系统应能给出判定结论：**全新知识** / **知识已被覆盖** / **部分新增**（已有条目 + 新信息）。
- 判定为"知识已被覆盖"的文章应被**归档**（不出现在浏览列表、不进入推送），但记录必须保留并可追溯。
- 判定为"部分新增"的文章应正常展示，同时把新增的知识点补充进 Wiki。
- 判定必须**保守**：证据不足或系统无法确定时，应视为"全新知识"（宁可多推，不可误筛）；
  这与既有"误合并比漏合并更严重"的原则一致——知识级误筛会让用户"看不到内容"，后果更重。
- 每条判定都应记录**依据**（命中的 Wiki 条目、相似度、模型与提示词版本），供维护者复核。
- 维护者应能人工修正：新增/编辑/废止/合并 Wiki 条目，或把某篇文章改判为"不是知识重复"或"确认为知识重复"。
- 人工修正的结果应**优先于**系统判定，并影响后续同类判定（已废止的条目不再作为覆盖依据）。
- 该能力应建立在既有"完全重复 → 近重复"两级去重**之后**：先合并文本重复，再做知识级判定，避免重复计算与误判。
- LLM 不可用（超时、限流、配额耗尽）时，系统**不得**中断采集、浏览与推送流程；
  相关文章应进入"待判定"状态并在后续轮次重试。
- 系统应能控制成本与用量：单篇/单轮调用额度、批量与并发上限、结果缓存，并可查询调用量与失败率。
- 维护者应能查看"因知识重复被筛掉"的文章清单与统计，以评估该能力是否过严或过松。
- **数据边界**：文章内容会发送给外部 LLM 服务用于提炼知识点，此点必须在用户可感知的位置（如站点说明）
  明确告知；系统不应把订阅者邮箱等个人信息发送给 LLM。

## 三、非目标（Out of Scope，说明性质，非技术方案）
- 本文档不涉及具体使用何种爬虫框架、分类算法、数据库或部署方式等技术实现细节。
- 本文档不涉及**用户账户体系**（注册/登录/密码）、社交互动（评论、点赞）等功能。
  - v2 的订阅功能通过**匿名订阅 + 邮箱双确认**实现，不需要账号；订阅者的身份仅由邮箱与确认/退订凭证表示。
- 本文档不涉及**多渠道推送**（短信、Webhook、站内信、RSS）：v2 只做电子邮件推送，其他渠道留待后续需求提出。
- 本文档不涉及"按个人阅读历史去重"：v2 的知识去重以**全局知识库**为准，
  即"这个知识点是否已被全站内容覆盖"，而不是"某个具体用户是否读过"。
- 本文档不涉及知识 Wiki 的对外协同编辑（多人协作、版本协商、权限体系），
  Wiki 仅由系统生成 + 维护者修正。
- 本文档不涉及自建/本地部署大模型：v2 使用外部 LLM API；模型选型、部署形态属于技术实现范畴。

## 四、变更记录

| 版本 | 变更 | 来源 | 影响范围 |
| --- | --- | --- | --- |
| v1 | 采集 / 分类 / 去重 / 浏览 / 维护（用户故事 1–9） | 初始需求 | `spec.md`、`plan.md`、`tasks.md`、`tickets.md` |
| v2 | 新增订阅与定期邮件推送（用户故事 10–13）、LLM Wiki 知识级去重（用户故事 14–16）；明确非目标边界 | 客户新增需求 | 本文档第二、三章；`plan.md` 技术栈/架构/API 契约；`tasks.md` 第 11–14 节 |

**v2 需求决策记录（已与客户确认）**

| 决策点 | 结论 | 理由 |
| --- | --- | --- |
| 推送渠道 | 邮件 | 触达面最广、无需用户侧集成；代价是需要处理确认/退订与投递合规 |
| 订阅者身份 | 匿名订阅（邮箱 + 双确认） | 满足"订阅感兴趣方向"的最小成本方案，避免引入账号体系扩大范围 |
| 知识去重粒度 | 全局知识库级 | 直接对应"筛选已看过的知识"这一诉求，与既有文本级去重互补 |
| 模型调用 | 外部 LLM API | 知识点提炼质量优先；通过"保守判定 + 人工复核"控制误筛风险 |

