# 项目治理原则（Constitution）

本文档定义本项目（自动爬取、分类整理与去重博客内容的网站）在代码质量、测试、用户体验一致性和性能方面的项目级治理原则。所有贡献者在开发、评审和合并代码时应遵循以下原则。

## 1. 代码质量标准

- **可读性优先**：代码应清晰表达意图，命名（变量、函数、模块）具备明确语义，避免晦涩缩写。
- **模块化与单一职责**：爬虫、内容解析、分类算法、去重逻辑、存储层、展示层应彼此解耦，各自独立可替换。
- **风格一致性**：项目统一使用 linter 与 formatter（如 ESLint/Prettier 或 Black/Flake8，视技术栈而定），并在 CI 中强制检查，不符合规范的代码不得合并。
- **代码评审（Code Review）**：所有合并到主分支的改动必须经过至少一名其他贡献者的评审，评审需关注正确性、可维护性与安全性。
- **文档化**：核心模块（爬虫策略、去重算法、分类模型）需配有说明文档，解释设计决策与关键假设。
- **依赖管理**：第三方依赖需评估维护活跃度与安全性，版本锁定并定期审查更新。
- **错误处理**：网络请求、解析、存储等易失败环节必须有明确的异常处理与日志记录，不允许静默失败。

## 2. 测试规范

- **测试分层**：
  - 单元测试覆盖核心逻辑（如去重算法、分类规则、URL 规范化）。
  - 集成测试验证爬虫—解析—存储—展示的端到端流程。
  - 针对爬虫的测试应使用录制/模拟的 HTML 响应（fixtures），避免测试依赖真实网络请求的不稳定性。
- **覆盖范围**：核心业务逻辑（去重、分类）测试覆盖率应达到项目设定的最低门槛（如 80% 以上），关键路径不允许无测试合并。
- **回归测试**：每次修复 Bug 后必须补充对应的回归测试用例。
- **持续集成**：所有 Pull Request 必须通过自动化测试流水线（CI）才能合并。
- **数据质量测试**：定期抽样检查爬取与分类结果的准确性，建立人工抽检或标注对比机制，防止分类模型/规则漂移。

## 3. 用户体验一致性要求

- **界面一致性**：全站沿用统一的设计规范（间距、字体、色彩、组件库），避免不同页面出现风格割裂。
- **交互一致性**：相同类型的操作（筛选、排序、分类切换、收藏等）在全站应遵循相同的交互模式与反馈方式。
- **内容呈现一致性**：博客摘要、来源标注、发布时间、分类标签等元信息的展示格式需全站统一。
- **响应式与可访问性**：界面需适配主流设备尺寸，并遵循基本的可访问性标准（如合理的对比度、键盘可操作性、语义化标签）。
- **状态反馈**：加载中、空结果、去重后合并、抓取失败等状态需有清晰、一致的提示文案与视觉表现。

## 4. 性能要求

- **爬取效率与礼貌性**：爬虫需遵守目标站点的 robots.txt 与访问频率限制，采用并发控制与限流策略，避免对目标站点造成过大压力。
- **去重与分类的可扩展性**：去重与分类算法应能在数据量增长时保持可接受的时间复杂度，必要时引入索引、哈希指纹或近似去重（如 SimHash/MinHash）等方案。
- **响应时间**：面向用户的页面（列表、搜索、分类浏览）首次内容加载应控制在可接受的延迟范围内（如 P95 < 2 秒，具体阈值可随项目迭代细化）。
- **资源利用**：后台任务（爬取、分类、去重）应与前台服务资源隔离，避免批处理任务影响用户访问体验。
- **可观测性**：关键性能指标（爬取耗时、队列积压、接口响应时间）需被监控与告警，以便及时发现性能退化。

---

*本文档为项目治理的顶层原则，后续具体的技术方案、编码规范细则、测试用例设计等文档应与本文档保持一致，如有冲突以本文档为准。*
